// Optional runtime override (CapRover/K8s can rewrite this file at start).
// CI / MinIO builds bake the API URL at build time via NEXT_PUBLIC_API_URL — leave empty here.
window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ ?? {};
